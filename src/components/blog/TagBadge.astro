---
import { getTagColor } from '../../config';

interface Props {
  tag: string;
}

const { tag } = Astro.props;
---

<span
  class:list={[
    'inline-flex items-center rounded-md border px-2.5 py-1 font-semibold leading-none m-1',
    getTagColor(tag),
  ]}
>
  {tag}
</span>
